self_healing:
  enabled: true
  mode: auto # 'auto' or 'manual'
  max_attempts: 3
  selector_preferences:
    - "data-test-id"
    - "data-testid"
    - "id"
    - "name"
    - "class"
    - "tag"
    - "xpath"
  # provider_keys: # Placeholder for future AI provider keys
  #   api_key: "YOUR_API_KEY"